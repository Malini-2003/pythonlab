import numpy as np

scores = np.array([87, 92, 75, 25, 99, 81, 60])

highest_score = np.max(scores)
lowest_score = np.min(scores)

print(f"Highest score: {highest_score}")
print(f"Lowest score: {lowest_score}")
